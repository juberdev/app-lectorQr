import 'package:flutter/material.dart';

class DireccionePage extends StatelessWidget {
  const DireccionePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('hola direcciones'),
    );
  }
}
